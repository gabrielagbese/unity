// In the Hierarchy, create an Empty object called “SpawnManager” 
// Create a new script called “SpawnManager”, attach it to the Spawn Manager, and open it
// Declare new public GameObject[ ] animalPrefabs; 
// In the Inspector, change the Array size to match your animal count, 
//      then assign your animals by dragging them from the Project window into the empty slots